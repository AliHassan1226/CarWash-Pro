import 'package:flutter/material.dart';

class MonitorOrdersScreen extends StatelessWidget {
  const MonitorOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Monitor Orders Screen')));
  }
}
